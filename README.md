Make sure you have uv installed:
`pip install uv`

Then run via uv:
`uv run main.py`

Example:
Green is source nodes, blue is intermediary nodes, red is sink nodes.
The self-loops represent the node capacity which may differ from the sum of its edge capacities.
<img width="1914" height="967" alt="image" src="https://github.com/user-attachments/assets/98bfa019-fbf9-439e-b5c2-dd8b8ce2cdd8" />
